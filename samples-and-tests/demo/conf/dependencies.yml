# Application dependencies

require:
    - play

    - formee -> formee

repositories:
    - test formee module:
        type:       local
        artifact:   ${application.path}/../../../[module]
        contains:
            - formee -> *
